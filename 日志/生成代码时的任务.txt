问题1：
界面后台代码生成分析
目前界面有复制功能，应该有CopyRecord 函数，#sym:Function4GeneCodeCRUDAi 类有虚函数CopyRecord，在扩展类要实现CopyRecord 函数，这样实现合理吗

问题2：
在界面后台为什么要生成这一行代码：
import { InUse_BindDdl_InUseInDivCache } from '@/ts/L3ForWApi/SysPara/clsInUseWApi';
还有这一行代码：是否是在功能区
 public async SetDdl_InUseInDivInFeature() {
    await InUse_BindDdl_InUseInDivCache(divVarSet.refDivFunction, 'ddlInUse');
  }